name: Get Pings

on:
  workflow_dispatch:

jobs:
  run:
    name: Run script to fetch pings
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - run: |
          npm install
          npm run prepare
          npm run start
