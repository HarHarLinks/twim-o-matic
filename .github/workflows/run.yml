name: Get Pings

on:
  workflow_dispatch:

jobs:
  run:
    name: Run script to fetch pings
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: MTRNord/ping-standalone
      - run: |
          npm install && npm run prepare && npm run start
